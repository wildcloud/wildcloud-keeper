node:
  name: master
  workers: 10

paths:
  tmp: /tmp
  images: /var/images
  mounts: /var/mounts
  temp: /tmp/images
  config: /etc/wildcloud/applications

amqp:
  :host: 127.0.0.1
  :port: 5672
  :user: guest
  :pass: guest
  :vhost: /